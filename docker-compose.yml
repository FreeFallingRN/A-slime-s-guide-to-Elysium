services:
  app:
    build: .
    container_name: slime_elysium_app
    volumes:
      - .:/workspace
      - /workspace/node_modules
    ports:
      - "5173:5173"
      - "5174:5174"
    environment:
      - NODE_ENV=development
